import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Login admin management",
  description: "Author Duongdt_dev",
};

export default function RegisterLayout() {
  return <Page />
}